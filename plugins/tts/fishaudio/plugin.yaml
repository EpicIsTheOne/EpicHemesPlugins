name: fishaudio-tts
version: 0.2.0
description: "Fish Audio TTS through the fish-audio-tts-toolkit helper, with automatic tagging, current model support, PCM output, and local wake-mode playback."
author: EpicIsTheOne
kind: backend
requires_env:
  - FISH_AUDIO_API_KEY